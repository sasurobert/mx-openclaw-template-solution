# ═══════════════════════════════════════════════════════════════════════════════
# mx-openclaw-template-solution — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
# INSTRUCTIONS:
#   1. Copy this file to .env:  cp .env.example .env
#   2. Fill in YOUR values
#   3. NEVER commit .env to Git (it's in .gitignore)
#
# For CI/CD deployment, set secrets via GitHub:
#   gh secret set WALLET_PEM < wallet.pem
#   gh secret set LLM_API_KEY --body "sk-..."
#   gh secret set VPS_SSH_KEY < ~/.ssh/id_ed25519
#   gh secret set VPS_HOST --body "203.0.113.42"
#   gh secret set VERCEL_TOKEN --body "vercel_xxxx"
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Agent Identity ────────────────────────────────────────────────────────────
AGENT_NAME=my-openclaw-bot
AGENT_DESCRIPTION="A MultiversX-powered AI agent"
AGENT_WALLET_ADDRESS=erd1...your_public_address

# ─── Pricing ───────────────────────────────────────────────────────────────────
PRICE_PER_QUERY=0.50
PRICE_TOKEN=USDC-350c4e

# ─── MultiversX Network ───────────────────────────────────────────────────────
# D = devnet, T = testnet, 1 = mainnet
MULTIVERSX_CHAIN_ID=D
MULTIVERSX_API_URL=https://devnet-api.multiversx.com
MULTIVERSX_EXPLORER_URL=https://devnet-explorer.multiversx.com

# ─── MX-8004 Contracts ────────────────────────────────────────────────────────
IDENTITY_REGISTRY_ADDRESS=erd1qqq...
VALIDATION_REGISTRY_ADDRESS=erd1qqq...
REPUTATION_REGISTRY_ADDRESS=erd1qqq...
ESCROW_CONTRACT_ADDRESS=erd1qqq...

# ─── Relayer (Gasless Transactions) ───────────────────────────────────────────
RELAYER_URL=http://localhost:3001

# ─── MCP Server ───────────────────────────────────────────────────────────────
MCP_URL=http://localhost:3000

# ─── Server ───────────────────────────────────────────────────────────────────
BACKEND_PORT=4000
FRONTEND_PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# ─── LLM Provider (Generic — works with OpenAI, Anthropic, etc.) ──────────────
# LLM_API_KEY=sk-...              ← Set via: gh secret set LLM_API_KEY
# LLM_PROVIDER=openai             ← openai | anthropic | gemini | local
# LLM_MODEL=gpt-4o                ← Model to use

# ─── IPFS (for Manifest Pinning) ──────────────────────────────────────────────
# PINATA_API_KEY=                  ← Optional: Pinata JWT
# PINATA_SECRET=

# ─── Deploy Secrets (⛔ NEVER put real values here!) ──────────────────────────
# WALLET_PEM                      ← gh secret set WALLET_PEM < wallet.pem
# VPS_SSH_KEY                     ← gh secret set VPS_SSH_KEY < ~/.ssh/id_ed25519
# VPS_HOST                        ← gh secret set VPS_HOST --body "1.2.3.4"
# VPS_USER                        ← gh secret set VPS_USER --body "deploy"
# VERCEL_TOKEN                    ← gh secret set VERCEL_TOKEN
# VERCEL_ORG_ID                   ← gh secret set VERCEL_ORG_ID
# VERCEL_PROJECT_ID               ← gh secret set VERCEL_PROJECT_ID
